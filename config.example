# Configuração do Banco de Dados
# Copie este arquivo para .env e configure as variáveis

# Use 'sqlite' para desenvolvimento ou 'postgresql' para produção
DATABASE_TYPE=sqlite

# Configurações PostgreSQL (usado apenas se DATABASE_TYPE=postgresql)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=controle_estoque
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# Caminho do banco SQLite (usado apenas se DATABASE_TYPE=sqlite)
DATABASE_PATH=database.db

