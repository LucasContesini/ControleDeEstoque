# Variáveis de Ambiente para Vercel
# ✅ SOLUÇÃO: Use Session Pooler do Supabase (IPv4 proxied for free)
# 
# No Supabase Dashboard → Settings → Database → Connection Pooling
# Copie a Connection String do Session Pooler (não Direct Connection)

# ============================================================================
# OPÇÃO 1: Usar DATABASE_URL (Recomendado - Mais Simples)
# ============================================================================
# Cole a Connection String completa do Session Pooler aqui:
# Formato: postgresql://postgres.PROJECT_REF:[PASSWORD]@aws-0-us-west-2.pooler.supabase.com:5432/postgres
# 
# Exemplo (substitua [YOUR-PASSWORD] pela sua senha):
DATABASE_URL=postgresql://postgres.htrghiefnoaytjmcdbuk:S%26mur%26i77681271@aws-0-us-west-2.pooler.supabase.com:5432/postgres

# ============================================================================
# OPÇÃO 2: Variáveis Individuais (Se não usar DATABASE_URL)
# ============================================================================

# Host do Session Pooler (IPv4-compatible)
DB_HOST=aws-0-us-west-2.pooler.supabase.com

# Porta do Session Pooler
DB_PORT=5432

# Nome do banco
DB_NAME=postgres

# User do Session Pooler (formato: postgres.PROJECT_REF)
DB_USER=postgres.htrghiefnoaytjmcdbuk

# Senha do banco de dados PostgreSQL (Supabase)
DB_PASSWORD=S&mur&i77681271

# Chave pública do Supabase (anon key) - para API REST
SUPABASE_KEY=sb_publishable_gSNmUBC5DQcx-UQKrFeGfw_wlbu27R9

# Chave secreta do Supabase (service_role key) - para upload de imagens
SUPABASE_SERVICE_KEY=sb_secret_ZjnLl9_3WQzamHBRZHNFhw_J5q2xyhD

# ============================================================================
# VARIÁVEIS OPCIONAIS (Só se API REST não funcionar)
# ============================================================================
# Se a API REST do Supabase não funcionar, o sistema tentará usar S3 como fallback.
# Nesse caso, descomente e configure as variáveis abaixo:

# SUPABASE_S3_ENDPOINT=
# SUPABASE_S3_ACCESS_KEY=
# SUPABASE_S3_SECRET_KEY=
# SUPABASE_S3_REGION=us-west-2

